/**
 * Shared size scale used across all components (Button, Input, Icon, etc.)
 * Aligned with the theme's t-shirt scale (spacing, fontSizes, borderRadius).
 */
export type ComponentSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
