import { View, Text } from 'react-native';
import React from 'react';

export const SelectboxComponent = (data,
  labelKey,

  selected,
  onChange,

  multiSelect = false,

  placeHolder,
  searchBar = false,
  searchPlaceholder = 'Search....',
  noResultsText,
  showChips = false,

  label
  disabled = false,
  error,
  required = false,

  style,
  testId,) => {
  return (
    <View>
      
    </View>
  );
};
